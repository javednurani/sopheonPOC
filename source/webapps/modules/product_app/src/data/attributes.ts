// this enum is controlled by Sopheon.CloudNative.Products.DataAccess.SeedData.ProductSeedData.cs (_defaultAttributes)
// keep this TS enum in sync with changes to the C# file ProductSeedData.cs

// eslint-disable-next-line no-shadow
export enum Attributes {
  INDUSTRIES = -1,
  NOTES = -2,
  DUEDATE = -3,
  STATUS = -4,
}
