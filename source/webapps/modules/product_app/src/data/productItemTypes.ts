// this enum is controlled by Sopheon.CloudNative.Products.Domain.ProductItemtype.cs (SystemManagedProductItemTypeIds)
// keep this TS enum in sync with changes to the C# enum

// eslint-disable-next-line no-shadow
export enum ProductItemTypes {
  TASK = -1,
}
